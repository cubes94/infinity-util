/**
 * {@link java.nio}
 * 定义了缓冲器，它是数据容器，并且提供其他NIO包的概述。
 * <p>
 * {@link java.nio.channels}
 * 定义了描述连接能够执行I/O操作的实体，例如文件和sockets。定义了多路复用且非阻塞I/O操作的选择器。
 * <p>
 * {@link java.nio.channels.spi}
 * {@link java.nio.channels}包的服务提供者类。
 * <p>
 * {@link java.nio.charset}
 * 定义了用于字节和统一编码字符之间转换的字符集、解码器和编码器。
 * <p>
 * {@link java.nio.charset.spi}
 * {@link java.nio.charset}包的服务提供者类。
 * <p>
 * {@link java.nio.file}
 * 定义了Java虚拟机访问文件、文件属性和文件系统的接口和类。
 * <p>
 * {@link java.nio.file.attribute}
 * 提供访问文件和文件系统属性的接口和类。
 * <p>
 * {@link java.nio.file.spi}
 * {@link java.nio.file}包的服务提供者类。
 */
package com.infinity.training.jdk.nio;